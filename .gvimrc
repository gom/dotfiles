if has("mac")
    set columns=160
    set lines=46
    set cmdheight=1

    set showtabline=2 " always show tab
    set transparency=10
    set antialias
    set guifont=Ricty:h14
    colorscheme desert
endif
